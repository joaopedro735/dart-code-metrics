name: dart_code_metrics_plugin_loader
description: This pubspec determines the version of the analyzer plugin to load.
version: 5.7.6

environment:
  sdk: ">=2.14.0 <3.0.0"

dependencies:
  dart_code_metrics:
    git:
      url: https://github.com/joaopedro735/dart-code-cmetrics
      ref: 5.7.7

dev_dependencies:
  lints: ^1.0.1
